// Знак мастерской: экран, по которому идёт полоса света.
//
// Это тот самый кадр, с которого начинается сайт: в motion.css экран
// «включается» — поперёк чёрной обложки растягивается линия, и створки
// разъезжаются. Знак — застывшая середина этого движения. Поэтому он и
// объясняет название: RESET — это устройство, которое снова включилось.
//
// Рамка берёт цвет у текста (currentColor), поэтому знак одинаково работает
// и на белой шапке, и на чёрном подвале. Цвет полосы света задаёт CSS:
// на белом нужен тёмный тон, на чёрном — светлый, иначе он не виден.
export function Logo() {
  return (
    <span className="logo__mark" aria-hidden="true">
      <svg width="24" height="24" viewBox="0 0 24 24">
        <rect
          x="2.9"
          y="4.4"
          width="18.2"
          height="15.2"
          rx="4"
          fill="none"
          stroke="currentColor"
          strokeWidth="1.8"
        />
        <path d="M7 12h10" strokeWidth="2.6" strokeLinecap="round" />
      </svg>
    </span>
  );
}
