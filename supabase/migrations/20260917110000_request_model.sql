-- Модель устройства отдельным полем.
-- Зачем: «iPhone» мастеру ничего не говорит. Экран у 13 и у 13 Pro разный,
-- и заказать деталь заранее можно только зная точную модель. Раньше всё
-- писали в одно поле device, и половина заявок приходила без модели.
-- Применяется командой: npm run db:push

-- Поле необязательное на уровне базы: старые заявки модели не содержат,
-- и not null сломал бы их. В форме на сайте поле обязательное — этого
-- достаточно, чтобы новые заявки приходили заполненными.
alter table public.repair_requests
  add column if not exists model text;

-- Политики RLS менять не нужно: колонка появляется внутри уже защищённой
-- таблицы и подчиняется тем же правилам, что и остальные поля заявки.
--
-- Публичный поиск по коду (request_status_by_code) перечисляет колонки
-- поимённо, модель туда не добавляем: клиенту она и так известна,
-- а лишнее поле в открытой функции — лишний риск.
